// This file is part of cgeo-shp, copyright (c) 2018 BusFaster Ltd.
// Released under the MIT license, see LICENSE.

import { Reader } from 'cbin';

import { Geometry as GeometrySHP } from './Geometry';
import { Parser, OptionsSHP, registerType } from './Parser';
import './Geometry';
import { Point } from './Point';
import { MultiLineString } from './MultiLineString';
/*
import './Polygon';
import './MultiPoint';
import './GeometryCollection';
*/
export { OptionsSHP, Parser };

for(let offset = 0; offset <= 20; offset += 10) {
	registerType(Point, offset + 1);
	registerType(MultiLineString, offset + 3);
//	registerType(Polygon, offset + 5);
//	registerType(MultiPoint, offset + 8);
}

declare module 'cgeo/dist/Geometry' {

	interface Geometry extends GeometrySHP {}

}
