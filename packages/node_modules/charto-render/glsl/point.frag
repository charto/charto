precision mediump float;

uniform float uSize;
uniform float uEdge;

varying vec4 vColor;

void main() {
	vec2 xy = gl_PointCoord * 2.0 - 1.0;
	float r = dot(xy, xy);
	float alpha = 1.0 - smoothstep(uEdge, 1.0, r);
	gl_FragColor = vColor * alpha;
}
