// This file is part of charto-model, copyright (c) 2017 BusFaster Ltd.
// Released under the MIT license, see LICENSE.

import { types as T, IModelType, ISimpleType, ISnapshottable } from 'mobx-state-tree';
import { mst, shim, action, ModelInterface } from 'classy-mst';

export const PointModelData = T.model({
	x: 0,
	y: 0
});

export class PointModelCode extends shim(PointModelData) {

	// TODO

}

export const PointModel = mst(PointModelCode, PointModelData);
export type PointModel = typeof PointModel.Type;
